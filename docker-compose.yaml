services:
  bot:
    build: .
    image: youtube-downloader-telegram
    container_name: youtube-downloader
    volumes:
      - .:/bot
    environment:
      - TOKEN
      - ALLOWED_USER_IDS
    restart: always
